void readLines(ifstream inPutLineFile, Line linesArray[],
const int MaxLnsSize, int numLines){
    
     inPutLineFile();

     while(inPutLineFile >> linesArray[numLines]){

     }
    
    MaxLnsSize = sizeof(linesArray[numLines])
}
